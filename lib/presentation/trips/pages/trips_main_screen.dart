import 'package:flutter/material.dart';

class TripsMainScreen extends StatelessWidget {
  const TripsMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
